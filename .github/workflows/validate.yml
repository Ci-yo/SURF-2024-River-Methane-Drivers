name: validate-public-analysis
on: [push, pull_request]
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
      - run: python scripts/build_atlas.py
      - run: python scripts/validate_repo.py
      - run: git diff --stat
